{
  "ignorePatterns": [
    {
      "pattern": "^http://localhost.*$"
    },
    {
      "pattern": "^.*medium.com.*$"
    }
  ],
  "retryCount": 5,
  "fallbackRetryDelay": "30s",
  "aliveStatusCodes": [
    200,
    429
  ]
}
